# URL of the Flask backend — used by Axios in the React app.
# When running inside Docker, the browser still calls localhost:5000
# because requests originate from the user's browser, not from inside the container.
VITE_API_URL=http://localhost:5000
